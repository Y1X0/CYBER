"""Protocol probes (Phase 6C.2) — one low-impact service-identification plugin per protocol.

Only `http` and `tls` exist (80/443). New protocols (SSH, databases, …) register here in later,
separately-approved phases without touching the orchestrator or core.
"""
